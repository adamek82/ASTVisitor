{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Launch ASTVisitor",
            "type": "cppvsdbg",  // ✅ Use MSVC debugger
            "request": "launch",
            "program": "${workspaceFolder}/bin/Debug/ASTVisitor.exe",  // ✅ Make sure this path is correct
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
        }
    ]
}
